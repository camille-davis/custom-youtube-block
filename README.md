# Fullsize Option for YouTube Embeds

Adds a "Fullsize Mode" toggle to YouTube embed blocks so they resize dynamically to match their container (which is how they already appear in the Gutenberg editor).
